export enum LocalStorage {
  AUTH_TOKEN = "authToken",
}

export enum API_URL {
  BASE_URL = "https://dev-abt1u4l9bzxp4ml.api.raw-labs.com/",
}

export enum API_ENDPOINTS {
  CUSTOMER_REVIEWS = "customer-reviews",
}
